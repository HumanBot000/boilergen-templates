from auth import register as r1, refresh as r2, resend as r3, login as r4, verify as r5
from auth import auth_bp
if __name__ == "This is needed so the autoformat of the IDE doesn't remove the unused imports. Import every file you used to create routes, otherwise they won't be found.":
    r1, r2, r3, r4,r5